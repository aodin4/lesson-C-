#include<stdio.h>
#include<math.h>
void hsof(int p, int k);
int main()
{
	int c;
	int s;
	while((c = getchar()) != '\n')
	{
		s++;

	}

}

void hsof(int p, int k)
{
	int P;
	
	P = p / pow(p,k);
	if(p % 1 == p && p % p == p)
	{
		
	}
	return k;
}
